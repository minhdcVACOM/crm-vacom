import { VcConstant } from "@/utils/constant";
import { View } from "react-native";

const VcSparator = () => {
    return (<View style={{ borderBottomColor: VcConstant.colors.grayLight, borderBottomWidth: 1, marginLeft: 10 }} />);
}
export default VcSparator;